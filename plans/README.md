# Implementation Plans

Generated by the improve skill on 2026-06-16. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

These plans were written against commit `227bfe9` with a dirty worktree already
present. Executors must run each plan's drift check before editing because some
current excerpts describe uncommitted code.

## Execution order & status

| Plan | Title                                | Priority | Effort | Depends on | Status                                                                                                |
| ---- | ------------------------------------ | -------- | ------ | ---------- | ----------------------------------------------------------------------------------------------------- |
| 001  | Bound `--exec` command capture       | P1       | M      | -          | TODO                                                                                                  |
| 002  | Harden renderer SVG attribute values | P1       | M      | -          | BLOCKED - executor edited shared dirty checkout; code passes verification but needs manual diff split |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale: finding fixed independently or approach
abandoned)

## Dependency notes

- Plans 001 and 002 are independent. Plan 001 touches command capture in
  `src/rich_card/runtime.py`; plan 002 touches renderer config/SVG attribute
  safety.
- If both plans are executed in one branch, run the full validation lane after
  each plan and again after both are complete.

## Findings considered and rejected

- Removing shell execution from `--exec`: rejected because `--exec` is an
  explicit documented feature; the risk to address is unbounded resource use,
  not the presence of shell execution itself.
- Replacing SVG image parsing: rejected because SVG image dimension parsing
  already uses `defusedxml.ElementTree.fromstring(...)` and embeds image bytes
  as escaped data URIs.
- Escaping source/title text: rejected as already handled by the current text
  rendering path; plan 002 focuses on renderer-provided SVG attributes instead.
- Dependency CVE status: not assessed in this run because `uv audit` requires
  network access to OSV and was not authorized.

## Shared validation lane

Use these commands for final verification unless a plan gives a narrower
intermediate command:

```bash
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python -m unittest discover
ruff check .
ruff format --check .
bandit -c pyproject.toml -r src scripts
```
